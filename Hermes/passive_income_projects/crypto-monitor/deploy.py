"""Deploy crypto monitor to Render/Railway free tier."""
print("""
Deploy options:
  1. Railway (free): railway login && railway init && railway up
  2. Render (free): Create Web Service from GitHub repo
  3. Replit: Import from GitHub, press Run

Set AGENTMAIL_API_KEY as environment variable in the dashboard.
The app runs on port 8000 by default.
""")
